function test()

addpath(genpath('src'))

% load
data_tmp = load('data.mat');
data_2d = data_tmp.data_2d;
data_3d = data_tmp.data_3d;

% extract 2d data
data_2d.ext_edge = extract_data(data_2d.ext_edge);
data_2d.int_edge = extract_data(data_2d.int_edge);
data_2d.surface = extract_data(data_2d.surface);

% extract 3d data
data_3d.ext_surface = extract_data(data_3d.ext_surface);
data_3d.int_surface = extract_data(data_3d.int_surface);
data_3d.ext_edge = extract_data(data_3d.ext_edge);
data_3d.int_edge = extract_data(data_3d.int_edge);
data_3d.volume = extract_data(data_3d.volume);

end

function data = extract_data(data)

% geom
data.geom = extract_geom(data.geom);

keyboard
data.Ex = value{1};
data.Ey = value{2};
data.E = value{3};

end
